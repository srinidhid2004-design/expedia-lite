# 02 — Environment setup

Follow CHECK → TAKE ACTION → VERIFY. Confirm compatible project-local Python, Node.js, npm, and the official create-vue requirements before adding dependencies. Create `backend/.venv`, declare only the backend packages the Part 1 application and tests require, and install them locally. Create a JavaScript Vue 3 frontend with ESLint using the official scaffolder. Do not copy an existing environment, dependency directory, or build output.

