# 01 — Assignment intake

Start a new Expedia Lite course project in the primary project folder. Read the complete attached assignment and safely inspect the travel-data ZIP before making changes. Extract the supplied data to `data/`, preserve the original CSV contents, and read the data README before interpreting columns.

Complete Part 1 only. The application may read `hotels.csv` and `trips.csv`; preserve but do not use `users.csv` or `bookings.csv`. Use the Hello Agent packet only for documentation and verification structure, never for calculator behavior or code.

