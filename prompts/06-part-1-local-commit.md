# 06 — Part 1 local commit

Work only inside the Expedia Lite project. Configure the repository-local Git identity as `Srinidhi Deekonda` and `srinidhi.d2004@gmail.com` without changing global Git configuration. Verify the exact local values before continuing.

Record the user's completed manual Visual Studio Code review in the evidence log. Correct the report and handoff to reflect the initialized `main` branch, the pre-checkpoint absence of a commit, remote, or push, and public GitHub publication as the next task. Keep the GitHub repository URL and exact implementation commit pending until publication.

Do not change application source, tests, dependencies, generated files, or instructor data. Confirm virtual environments, dependency directories, and build output are ignored. Stage and review all intended Part 1 project files, run `git diff --cached --check`, and stop if anything unintended or generated is staged. Otherwise, create exactly one local commit with subject `Complete Expedia Lite Part 1`.

Verify the full commit SHA, subject, `main` branch, clean working tree, complete committed-file list, excluded generated directories, and absence of any remote, push, merge, rebase, GitHub repository operation, or global Git configuration change. Stop after the local commit.
