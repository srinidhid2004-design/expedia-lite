# 07 — GitHub publication and Part 1 report finalization

Work only inside Expedia Lite. Verify the clean reviewed `main` branch, preserve implementation commit `0c1666d2bb03fdefda55ccf3b905d80801d78d5f`, confirm its subject, ensure no remote exists, and confirm both committed screenshots are present.

Using browser control, identify the signed-in GitHub account, confirm that `expedia-lite` does not already exist, and create a public empty repository without generated content. Capture its HTTPS URL, add it as local `origin`, and push with `git push -u origin main` without force. Stop for manual sign-in, two-factor authentication, CAPTCHA, or Git credential interaction when required. Verify the reviewed implementation commit publicly before editing documentation.

Finalize `report.md` with the public repository, full implementation SHA, concise Vue/FastAPI/CSV flow, completed manual review, explicit successful-search and no-results actions with expected and observed results, immutable implementation-commit screenshot links, public project-document links, genuine limitations, and Part 2 explicitly unimplemented. Update the evidence log and current handoff, then add this numbered prompt record.

Confirm that only documentation changed, stage only those files, run `git diff --cached --check`, commit with subject `Finalize Part 1 submission report`, and push `main` normally. Verify the public repository, both commits, tracking branch, clean working tree, report, project-document links, and screenshot links without relying on authentication. Do not access or submit anything to the course site.
